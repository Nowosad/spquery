% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spq_proximity.R
\name{spq_proximity}
\alias{spq_proximity}
\title{Find the distance to the closest feature-space values in another raster}
\usage{
spq_proximity(
  x,
  y,
  dist_fun,
  output = c("distance", "id", "all"),
  block_size = 100,
  progress = TRUE,
  dist_approx = FALSE,
  eps = 0,
  ...
)
}
\arguments{
\item{x}{An object of class SpatRaster (terra). Its cells define the query
feature vectors and its geometry is retained in the result.}

\item{y}{An object of class SpatRaster (terra). Its cells define the
reference feature vectors.}

\item{dist_fun}{Distance measure used. This function uses the same distance
measures as \code{\link[=spq_search]{spq_search()}}, including measures from \code{philentropy},
\code{proxy}, and dynamic time warping through \code{dtwclust}. Set
\code{dist_approx = TRUE} with \code{dist_fun = "euclidean"} to use an approximate
Euclidean nearest-neighbour search.}

\item{output}{Character string specifying the output: \code{"distance"} returns
the distance to the closest feature vector, \code{"id"} returns the 1-based
cell ID of the closest cell in \code{y}, and \code{"all"} returns both as layers.}

\item{block_size}{Number of rows from \code{x} to process at once.}

\item{progress}{Logical; show a progress bar while processing \code{x}?}

\item{dist_approx}{Logical; use an approximate nearest-neighbour method?
Currently supported for \code{dist_fun = "euclidean"} only.}

\item{eps}{Non-negative, dimensionless relative approximation tolerance for
approximate Euclidean search. For example, \code{eps = 0.1} permits a result
approximately up to 10 percent farther than the exact nearest distance.
Larger values can be faster but less accurate. The default, zero, requests
an exact nearest-neighbour search. It has no raster or feature-value units.}

\item{...}{Additional arguments for the selected distance backend. When
\code{dist_fun = "dtw"}, \code{ndim} must be supplied.}
}
\value{
An object of class SpatRaster (terra) with the same geometry as
\code{x}. It has one layer for \code{output = "distance"} or \code{output = "id"}, and
two layers, named \code{distance} and \code{id}, for \code{output = "all"}.
}
\description{
For each cell in \code{x}, finds the minimum distance to any complete feature
vector in \code{y}. Raster geometry is not used in the calculation; the layers
of each raster represent the dimensions of a feature vector.
}
\examples{
library(terra)
ta = rast(system.file("raster/ta_scaled.tif", package = "spquery"))
pr = rast(system.file("raster/pr_scaled.tif", package = "spquery"))

re = spq_proximity(
  ta, pr, dist_fun = "euclidean", dist_approx = TRUE
)
plot(re)
}
